apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: gateway-api
  namespace: flux-system
spec:
  interval: 24h
  ref:
    # Pin to exact commit (v1.4.1 release). Update this to upgrade Gateway API CRDs.
    commit: 477d172e6ac5eccb82b65781ddb8f924afec4170  # v1.4.1
  url: https://github.com/kubernetes-sigs/gateway-api
